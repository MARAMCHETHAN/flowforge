# Daily Update — 2026-07-07 (FlowForge)

## Goal today
"Remove the perspective that I'm doing this for them — take the idea and
build something of my own I can show as a personal project."

## Decisions (user's, via Q&A)
- Name: **FlowForge** (over PixelFlow / Arcade Pipelines)
- LLM backend: **multi-provider** (OpenAI + Anthropic + Gemini)
- Location: **new folder** `~/Desktop/flowforge` — original assessment folder untouched

## What was done

### 1. Fork + scrub
- Copied frontend/src+public, backend, learn/ to `~/Desktop/flowforge`
  (node_modules APFS-cloned; no .git, no PDF, no Loom-script docs).
- Rebranded everything user-visible: toolbar "FLOWFORGE ◆ PIPELINES" + "F"
  mark, index.html title, manifest, ⌘K footer, export filename, FastAPI
  title, localStorage key → `flowforge-pipeline-v1`, package name.
  `vs-` CSS class prefixes intentionally kept (pure churn to rename).
- learn/ docs rewritten: all "Assessment Part N" / VectorShift / take-home /
  interview framing removed; 07 rewritten around the new provider layer;
  verified `grep -ri assessment|vectorshift|take-home` = clean.

### 2. NEW: multi-provider LLM layer (`backend/providers.py`)
- `MODEL_REGISTRY`: display names → (provider, api id): GPT-4o(-mini),
  Claude Sonnet 4.6 / Haiku 4.5, Gemini 2.5 Pro / Flash.
- Raw httpx calls (no SDKs, symmetric across providers):
  - OpenAI: `POST /v1/chat/completions`, Bearer auth
  - Anthropic: `POST /v1/messages`, `x-api-key` + `anthropic-version:
    2023-06-01`, temperature clamped to ≤1.0 (checked against current
    Claude API reference via the claude-api skill)
  - Gemini: `:generateContent` with `system_instruction` + `generationConfig`
- Fallback chain: no key → simulation (logged); API error → simulation
  (error logged in node trace). Simulation moved out of main.py.
- Frontend model dropdowns (LLM, PromptTemplate, templates.js) updated to
  match the registry; Llama/Claude-3.x options removed.

### 3. Repo setup
- Root README with 4 screenshots (docs/screenshots/), architecture diagram,
  quickstart, feature table. MIT LICENSE. .gitignore. Fresh `git init -b
  main`, single clean commit (52 files). frontend/README replaced (was CRA
  default).

### 4. Verification
- `pytest test_main.py`: **17/17** (14 original + 3 new provider tests;
  autouse fixture deletes provider env keys so tests can never make live
  calls).
- Execute smoke with "Claude Haiku 4.5", no key: correct simulation +
  `no ANTHROPIC_API_KEY set — using simulation` in trace.
- Playwright driver (reused from old repo, drives :3000 generically):
  smoke / cmdk / cycle / textvars **all green** against FlowForge; modal
  screenshot confirms new branding + "Claude Sonnet 4.6 · simulated" output.
- Gotcha hit: `pkill react-scripts` left the OLD frontend on :3000 → had to
  `lsof -ti :3000 | xargs kill -9`; verified via `<title>` before driving.

## State at end of day
- Servers running: FlowForge backend :8000, frontend :3000.
- Live-call path NOT yet exercised (no API key exported during session) —
  first thing to test when a key is available.

## Next
1. Push to GitHub; deploy (Vercel frontend + Render/Fly backend).
2. Test live path with GEMINI_API_KEY (free tier).
3. Run-time input prompt dialog on Run.
4. Fix known bug: edges linger when a `{{variable}}` is deleted from a Text
   node (documented in learn/08).

---

## Session 2 (same day) — Learn Mode

User direction: "make it a teaching front where users with zero knowledge
understand it and use it" + recheck everything.

### Recheck
- 17/17 pytest, clean tree, both servers healthy, webpack clean.

### Fixed
- The known `{{variable}}` bug: deleting a variable from a Text node now
  prunes edges into the vanished handle (`updateNodeField` in store.js).
  learn/08 limitation entry updated to reflect the fix.

### NEW: Learn Mode (the differentiator)
- `frontend/src/learnMode.js`: quest-log tutorial panel (bottom-left).
  6 steps, each with a concrete instruction + a 💡 concept explainer
  (what an LLM is, what edges are, why Output exists). Steps complete
  THEMSELVES by watching store state (nodes/edges/runStatuses) — no
  "Next" buttons. Celebration screen on completion; progress/dismissal
  in localStorage `flowforge-learn-v1`; auto-opens on first visit with
  empty canvas; 🎓 LEARN toolbar button reopens it.
- Plain-English run narration: results modal now leads with
  "WHAT HAPPENED (PLAIN ENGLISH)" — one sentence per executed node
  ("The INPUT node 'document' handed your text to the pipeline…"),
  including simulated-vs-live LLM wording and router branch taken.

### Verified
- Driver all green with Learn Mode active; screenshots confirm panel on
  empty canvas + narration in modal. Screenshots refreshed in README.
- Committed: 2b7b221.

### Positioning note (from discussion)
FlowForge vs n8n/Langflow: closest relatives are Langflow/Flowise, not
n8n (trigger-based automation). Differentiators: per-node observability,
keyless simulated demo, design identity, and now TEACHING — no
competitor onboards zero-knowledge users. Pitch: "learn LLM pipelines
by building one."

---

## Session 3 — live Gemini verified ✅
- User created a Gemini key (new "AQ.A..." AI Studio format, not "AIza") →
  stored in backend/.env (gitignored). Backend started with `set -a; source .env`.
- Live pipeline verified end-to-end: Input → "Gemini 2.5 Flash" → Output
  returned a real model answer; trace shows "live response from gemini
  (gemini-2.5-flash)".
- Learning: Gemini 2.5 models spend output budget on internal thinking —
  maxTokens 128 truncated the answer mid-sentence; 1024 (the node default)
  is fine. Consider a floor or hint in the LLM node later.
- Deploy prep committed earlier (2b18cc4): REACT_APP_API_URL, ALLOWED_ORIGINS,
  render.yaml, .env.example. Next: GitHub push → Render → Vercel.

## Session 3b — real file uploads
- User hit the gap: FileUpload only stored metadata; backend substituted
  simulated text, so "summarize my file" summarized a placeholder.
- Fixed: BaseNode file field now FileReader.readAsText()s plain-text files
  (.txt/.md/.csv/.json, 200KB cap) into node.data.file.content; backend
  _run_file_upload emits real content when present, honest simulated
  fallback (with explanatory log) for PDF/DOCX. nodeMeta ⓘ copy updated.
- 19/19 pytest (2 new tests). Live proof: trip_notes.txt → Text
  "Summarize: {{doc}}" → Gemini 2.5 Flash → correct real summary.
- PDF/DOCX real extraction = future work (needs pdfjs/mammoth client-side
  or server-side upload endpoint).

## Session 4 — GitHub published
- gh CLI installed (2.96.0), user authed as MARAMCHETHAN.
- Repo created + pushed: https://github.com/MARAMCHETHAN/flowforge (public).
- Verified: .env NOT in repo (only .env.example); screenshots present.
- Remaining: Render (blueprint) + Vercel (frontend) dashboard steps.

## Session 5 — Render backend live (+ security incident handled)
- Backend deployed: https://flowforge-api-x2a9.onrender.com (blueprint).
- INCIDENT: first deployed test hit a transient Google 503; the error log
  embedded the full request URL INCLUDING ?key=... → API key leaked into
  the public run trace. Fixed same hour (commit "Security: never leak API
  keys into run traces"): Gemini key moved to x-goog-api-key header; logs
  now report only HTTP status/exception class; +1 retry on 429/5xx.
- Redeployed + verified: live Gemini answer from Render, clean logs.
- User advised to rotate the Gemini key (old one exposed) and update
  Render env + local backend/.env.
- Remaining: Vercel frontend (step 4), then ALLOWED_ORIGINS on Render.

## Session 6 — DEPLOYED 🎉
- Frontend: https://flowforge-sigma-eosin.vercel.app (Vercel, root=frontend,
  preset had to be switched FastAPI→Create React App; the hashed
  *-projects.vercel.app URLs are SSO-protected previews — share the
  production domain only).
- Backend: https://flowforge-api-x2a9.onrender.com (Render blueprint,
  GEMINI_API_KEY + ALLOWED_ORIGINS=<vercel url> set in dashboard).
- Final e2e verified: CORS preflight OK from Vercel origin; live Gemini
  answer through the full public stack.
- README now carries the live-demo link.

## Session 7 — cold-start UX
- User hit "Could not reach the backend" in the deployed app: Render free
  tier was asleep; frontend failed on the first 5xx during wake-up.
- Fix in submit.js: Run now retries up to 5x (8s apart) with a visible
  "WAKING SERVER… (FREE HOSTING)" button state; final error message
  explains the sleep behavior instead of implying the server is broken.

## Session 8 — clean code pass + CI
- Backend split: main.py (479 lines, 5 jobs) → main.py 175 (app+endpoints) /
  graph.py 61 (Kahn) / executors.py 160 (node runners) / engine.py 106
  (execution loop). providers.py untouched. 19/19 pytest unchanged.
- Frontend: {{variable}} regex deduped from 3 copies into
  src/utils/variables.js (stateful-regex-safe); submit.js (372) split into
  runResults.js (modal+trace+story) and submit.js (button logic, named
  wake constants).
- NEW: 9 frontend unit tests (variables parsing, buildStory narration) —
  first Jest tests in the project.
- NEW: GitHub Actions CI (.github/workflows/ci.yml): backend pytest +
  frontend jest + CI=true build, on push/PR. README badge added.
- Full verification: 19 pytest + 9 jest + driver all green.

## Session 9 — beginner UX pass (user request: zero-knowledge walkthrough)
Problems found & fixed:
1. ⌘K labels meaningless on Windows → utils/platform.js MOD_K, used in
   empty state, palette footer, Learn hints (handlers already accepted Ctrl).
2. Clicking a toolbar chip did nothing (beginners click, not drag) →
   click-to-add: chip click → store.pendingAddType → canvas places node at
   viewport center with jitter. Verified via Playwright: 1 click = 1 node.
3. No discoverable way to delete a wrong wire → double-click an edge
   deletes it; Learn step 3 hint mentions it.
4. Learn connect-step passed on Input→System wiring (then empty LLM answer)
   → check now requires the -prompt handle; hint says LOWER dot, why
   explains System vs Prompt.
5. Run with no OUTPUT connected: silent → modal now shows OUTPUTS section
   hint ("Nothing arrived at an OUTPUT node…").
6. Tiny connection dots → .vs-handle enlarged to 14px.
7. Disabled RUN unexplained → tooltip pointing at 🎓 LEARN.
8. "Default Value" jargon → "Text to send" (node label + Learn hint).
9. Broken-on-mobile with no warning → fixed banner under 820px.
10. CRA leftovers: React favicon + "Web site created using
    create-react-app" meta → pixel-F favicon.svg, honest description,
    og:title/description.
Verified: 9 jest + driver all green + custom Playwright click-to-add probe.

## Session 10 — "worthy" pass: visibility + protection
- GitHub repo metadata: 8 topics + homepage (live URL) via gh repo edit.
- Keep-warm workflow: GitHub Actions cron pings Render every 10 min.
- Rate limiting: per-IP sliding window on /pipelines/execute (10/min,
  env-tunable), proxy-aware (x-forwarded-for). Protects the public Gemini
  quota. Frontend stops retrying on 429 and shows the limit message.
  +1 test (20/20).
- Demo GIF (docs/demo.gif, 0.83MB, 8 frames): Playwright replays the full
  beginner journey — click-to-add nodes, wire them, pick Gemini 2.5 Flash,
  type text, RUN → LIVE Gemini answer ("Robots are developing cooking
  skills.") with Learn Mode ticking 0/6→6/6. Now the README hero.
  (Recording found a real overlap: nodes placed under the Learn panel
  can't be clicked — cosmetic, users can drag nodes/close panel.)
- All layers verified: 20 pytest, 9 jest, driver 4/4.
