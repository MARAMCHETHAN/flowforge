# Daily Update — 2026-07-07 (FlowForge)

## Goal today
"Remove the perspective that I'm doing this for them — take the idea and
build something of my own I can show as a personal project."

## Decisions (user's, via Q&A)
- Name: **FlowForge** (over PixelFlow / Arcade Pipelines)
- LLM backend: **multi-provider** (OpenAI + Anthropic + Gemini)
- Location: **new folder** `~/Desktop/flowforge` — original assessment folder untouched

## What was done

### 1. Fork + scrub
- Copied frontend/src+public, backend, learn/ to `~/Desktop/flowforge`
  (node_modules APFS-cloned; no .git, no PDF, no Loom-script docs).
- Rebranded everything user-visible: toolbar "FLOWFORGE ◆ PIPELINES" + "F"
  mark, index.html title, manifest, ⌘K footer, export filename, FastAPI
  title, localStorage key → `flowforge-pipeline-v1`, package name.
  `vs-` CSS class prefixes intentionally kept (pure churn to rename).
- learn/ docs rewritten: all "Assessment Part N" / VectorShift / take-home /
  interview framing removed; 07 rewritten around the new provider layer;
  verified `grep -ri assessment|vectorshift|take-home` = clean.

### 2. NEW: multi-provider LLM layer (`backend/providers.py`)
- `MODEL_REGISTRY`: display names → (provider, api id): GPT-4o(-mini),
  Claude Sonnet 4.6 / Haiku 4.5, Gemini 2.5 Pro / Flash.
- Raw httpx calls (no SDKs, symmetric across providers):
  - OpenAI: `POST /v1/chat/completions`, Bearer auth
  - Anthropic: `POST /v1/messages`, `x-api-key` + `anthropic-version:
    2023-06-01`, temperature clamped to ≤1.0 (checked against current
    Claude API reference via the claude-api skill)
  - Gemini: `:generateContent` with `system_instruction` + `generationConfig`
- Fallback chain: no key → simulation (logged); API error → simulation
  (error logged in node trace). Simulation moved out of main.py.
- Frontend model dropdowns (LLM, PromptTemplate, templates.js) updated to
  match the registry; Llama/Claude-3.x options removed.

### 3. Repo setup
- Root README with 4 screenshots (docs/screenshots/), architecture diagram,
  quickstart, feature table. MIT LICENSE. .gitignore. Fresh `git init -b
  main`, single clean commit (52 files). frontend/README replaced (was CRA
  default).

### 4. Verification
- `pytest test_main.py`: **17/17** (14 original + 3 new provider tests;
  autouse fixture deletes provider env keys so tests can never make live
  calls).
- Execute smoke with "Claude Haiku 4.5", no key: correct simulation +
  `no ANTHROPIC_API_KEY set — using simulation` in trace.
- Playwright driver (reused from old repo, drives :3000 generically):
  smoke / cmdk / cycle / textvars **all green** against FlowForge; modal
  screenshot confirms new branding + "Claude Sonnet 4.6 · simulated" output.
- Gotcha hit: `pkill react-scripts` left the OLD frontend on :3000 → had to
  `lsof -ti :3000 | xargs kill -9`; verified via `<title>` before driving.

## State at end of day
- Servers running: FlowForge backend :8000, frontend :3000.
- Live-call path NOT yet exercised (no API key exported during session) —
  first thing to test when a key is available.

## Next
1. Push to GitHub; deploy (Vercel frontend + Render/Fly backend).
2. Test live path with GEMINI_API_KEY (free tier).
3. Run-time input prompt dialog on Run.
4. Fix known bug: edges linger when a `{{variable}}` is deleted from a Text
   node (documented in learn/08).
